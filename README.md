# Jarvis Coach V0.1

Mobile-first behavioral task coach prototype.

## Local dev
npm install
npm run dev

## Deploy
Import this repository into Vercel. Build command: `npm run build`. Output directory: `dist`.
